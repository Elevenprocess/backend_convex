// Fichier temporaire de debug (repro /client, 2026-07-23) — neutralisé, à
// supprimer. (La suppression directe a été refusée par les permissions de la
// session ; le contenu d'origine dépendait de dumps locaux hors dépôt.)
import { describe, it } from 'vitest'

describe.skip('repro /client (obsolète)', () => {
  it('noop', () => {})
})
